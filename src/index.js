// import `.scss` files
import './scss/styles.scss';

// import App class
import { App as defaultExport } from './lib/app';

// export default App class
// I used `defaultExport` to state that variable name doesn't matter
export default defaultExport;
